{
  "name": "atlas-rentals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/pricing.js && node --check js/enquiry.js && node --check js/app.js"
  }
}
